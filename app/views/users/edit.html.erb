
<div class="container py-5">
  <div class="row">
    <div class="col col-md-8 offset-md-2">
      <h2><%= @user.slug %> > Edit</h2>
      <%= link_to raw('&larr; Back to user'), user_path(@user) %>
      <%= render 'form', user: @user %>
      <%= link_to raw('&larr; Back to user'), user_path(@user) %>
    </div>
  </div>
</div>
