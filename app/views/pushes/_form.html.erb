<%= form_with(model: push, local: true) do |form| %>
  <% if push.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(push.errors.count, "error") %> prohibited this push from being saved:</h2>

      <ul>
      <% push.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :repository_id %>
    <%= form.text_field :repository_id, id: :push_repository_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :push_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
