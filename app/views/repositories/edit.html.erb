
<div class="container py-4">
  <div class="row">
    <div class="col col-md-8 offset-md-2">
      <h2><%= @repository.uuid %> > Edit</h2>
      <%= link_to raw('&larr; Back to repository'), user_repository_path(@repository.user, @repository) %>
      <%= render 'form', repository: @repository, user: @repository.user %>
    </div>
  </div>
</div>
