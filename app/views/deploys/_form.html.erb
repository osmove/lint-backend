<%= form_with(model: deploy, local: true) do |form| %>
  <% if deploy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deploy.errors.count, "error") %> prohibited this deploy from being saved:</h2>

      <ul>
      <% deploy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :repository_id %>
    <%= form.text_field :repository_id, id: :deploy_repository_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :deploy_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
