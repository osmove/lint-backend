one:
  email: one@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>
  username: one
  slug: one
  authentication_token: token-one
  confirmed_at: <%= Time.current %>
  number_of_seats: 1

two:
  email: two@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, "password123") %>
  username: two
  slug: two
  authentication_token: token-two
  confirmed_at: <%= Time.current %>
  number_of_seats: 1
